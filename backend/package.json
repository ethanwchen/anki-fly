{
  "name": "drosophil-anki-friends-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Friends / presence / weekly race backend for the Drosophil-Anki add-on (Cloudflare Worker + KV)",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@cloudflare/workers-types": "^5.20260918.1",
    "typescript": "^5.6.0",
    "vitest": "^4.1.0",
    "wrangler": "^4.0.0"
  }
}
