name: release
on:
  push:
    tags: ['v*']
jobs:
  build:
    runs-on: ubuntu-latest
    permissions: { contents: write }
    steps:
      - uses: actions/checkout@v4
      - name: Build .ankiaddon
        run: ./build.sh
      - name: Run sim tests
        run: node tests/test_sim.mjs && node tests/test_circuits.mjs
      - uses: softprops/action-gh-release@v2
        with:
          files: dist/anki-fly.ankiaddon
          generate_release_notes: true
